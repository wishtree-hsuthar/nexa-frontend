name: Claude AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  claude-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get PR Diff
        run: |
          git fetch origin ${{ github.base_ref }}
          git diff origin/${{ github.base_ref }} > pr.diff
          head -c 20000 pr.diff > pr_trimmed.diff

      - name: Call Claude API
        run: |
          RESPONSE=$(curl https://api.anthropic.com/v1/messages \
            -s \
            -H "Content-Type: application/json" \
            -H "x-api-key: ${{ secrets.CLAUDE_API_KEY }}" \
            -H "anthropic-version: 2023-06-01" \
            -d @- <<EOF
          {
            "model": "claude-3-haiku-20240307",
            "max_tokens": 800,
            "messages": [
              {
                "role": "user",
                "content": [
                  {
                    "type": "text",
                    "text": "Review this PR diff and give actionable feedback:\n\n$(cat pr_trimmed.diff)"
                  }
                ]
              }
            ]
          }
          EOF
          )

          echo "$RESPONSE" > response.json

      - name: Extract Claude Response
        id: extract
        run: |
          COMMENT=$(jq -r '.content[0].text // "No response from Claude"' response.json)
          echo "comment<<EOF" >> $GITHUB_OUTPUT
          echo "$COMMENT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Post Comment on PR
        uses: actions/github-script@v7
        with:
          script: |
            const comment = `${{ steps.extract.outputs.comment }}`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "## 🤖 Claude AI Review\n\n" + comment
            });
